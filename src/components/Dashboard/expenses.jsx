import { Box } from "@mui/material";

export const Expenses = () => {
  return (
    <Box
      sx={{
        display: "flex",
        backgroundColor: "white",
        color: "black",
        width: "40%",
        height: "100%",
        borderRadius: "5px",
      }}
    ></Box>
  );
};

export default Expenses;
