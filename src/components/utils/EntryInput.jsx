import { OutlinedInput, styled } from "@mui/material";

const EntryOutlinedInput = styled(OutlinedInput)({
  width: "100%",
  height: "45px",
  borderRadius: "8px",
});

export default EntryOutlinedInput;
