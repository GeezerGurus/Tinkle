import { Box } from "@mui/material";

export const Chart = () => {
  return (
    <Box
      sx={{
        display: "flex",
        backgroundColor: "white",
        color: "black",
        width: "40%",
        height: "100%",
        borderRadius: "5px",
      }}
    ></Box>
  );
};

export default Chart;
