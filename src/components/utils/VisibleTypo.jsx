import { styled, Typography } from "@mui/material";

export const VisibleTypo = styled(Typography)(() => ({
  color: "black",
  fontWeight: "600",
  fontSize: "15px",
}));

export default VisibleTypo;
